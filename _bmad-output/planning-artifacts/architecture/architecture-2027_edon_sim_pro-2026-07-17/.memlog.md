---
scope: e-DON Lesson Studio full-platform architecture: edon-lesson repo (schema, backend, player, authoring, docs) + mod_edonlesson + block_edon_ai work item; external systems by API contract only
purpose: Build substrate for epics/stories; ADR set (ADR-001+); six V3-readiness seams demonstrated as extensions; four UX handoff items resolved
altitude: initiative (keeps the PRD's features/epics coherent)
updated: 2026-07-18T04:26
---

- (event) Run started, fast path per stakeholder directive (john, 2026-07-17): scope fenced by final PRD; UX Architect handoff consumed first; decide-and-record where silent, stop-and-ask on conflict; batched end-of-run confirmations; artifacts not final without sign-off.
- (constraint) [HARD] inherited verbatim from project-context.md (authoritative, to be EXTENDED never weakened): generate-once economics; review gate; allowlist SVG sanitisation; simulation iframe sandbox + strict CSP; tenant isolation everywhere; no wildcard CORS; single provider-agnostic OpenAI-compatible LLM adapter with per-workload config; cost telemetry on every LLM call; clean-room vs OpenMAIC/any copyleft; permissive deps only (GPLv3 confined to mod_edonlesson); no TypeScript; Python 3.12; PostgreSQL + tenant-scoped object storage; VPS/nginx/systemd/Lets-Encrypt deploy; schema-first versioned Lesson Script; async generation; immutable publications; six V3 seams preserved; repo layout section 8; ADR-001/ADR-002 reserved.
- (constraint) Scope fence: final PRD FR-1..FR-29 + stakeholder-approved UX additions only (schema altText/longDescription, sanitiser a11y allowlist preserving title/desc/aria-*, FR-27 extensions: diagram review completed + diagram invalidated, per-Block generation progress events, Showcase tier detection + enhancement chunks). No feature additions or removals.
- (constraint) External systems are API contracts only: edon-rag (never modified - capability gaps become integration work items documented in /docs/integrations), Moodle 5.x (grade/completion/web-service APIs via mod_edonlesson only), block_edon_ai (modifiable companion, minimal render-only surface per OQ-12).
- (decision) Paradigm: MODULAR MONOLITH backend - one deployable FastAPI-or-Flask app (per ADR-001) plus worker processes sharing a single core package - with HEXAGONAL ports-and-adapters at every external boundary (LLM adapter, edon-rag client, storage driver, LMS-agnostic platform API); generation pipeline is PIPES-AND-FILTERS with stages/prompts as versioned config data; Player is a REGISTRY-BASED block-renderer shell. Binds all. Prevents: service sprawl on a single VPS, and boundary leakage that would close the V3 seams.
- (decision) HANDOFF 11 RESOLVED - Attempt unit: an attempt is one full Lesson run pinned to one Published Version. Consumed only at first Quiz submission of a run (view-only runs never consume; lessons with zero Quiz Blocks have unlimited runs and attempt limits are moot). Reload/resume re-attaches to the open attempt server-side (keyed student+lesson) - never ends the pinned session, never consumes a new attempt. Per-Quiz-Block submissions live INSIDE the attempt. Cross-version scoring: each attempt scores earned/possible against ITS OWN version quiz denominator, normalised to a fraction; gradebook receives max fraction across attempts x Moodle activity grade-max; attempt counts are per-Lesson not per-version. Retake is an explicit post-completion action, loads latest Published Version, allowed while attempts_used < limit (limit configured in Moodle Lesson picker, passed via playback session).
- (decision) HANDOFF 16 RESOLVED - Launch Token lifecycle: JWT HS256 signed with per-tenant secret (kid header, two active keys per tenant for zero-downtime rotation); claims iss=platform-tenant-id, sub=moodle-user-id, role, courseid, optional draft return hint, iat, exp=iat+120s, jti. Single-use: jti recorded in a replay table until exp. Token is EXCHANGED at Authoring UI boot for an Authoring Session (HttpOnly SameSite=Lax cookie, server-side session): 8h absolute lifetime, no idle timeout. Pre-expiry warning banner at T-15min (lifetime known client-side from session bootstrap). Autosave is continuous and independent of session end; unsent editor content is preserved in browser localStorage keyed by draft id and restored after relaunch. Relaunch-into-same-Draft: Authoring UI stores last-active-draft client-side and platform records it server-side per (tenant,user); a fresh launch with a valid token redirects to that Draft when it exists and pending local edits or the server record point at it.
- (decision) HANDOFF 17 RESOLVED - Quiz submission durability: Submit sends a client-generated submission UUID; server endpoint is idempotent on (attempt_id, block_id, submission_uuid) and persists the submission + server re-score in one PostgreSQL transaction BEFORE acking; the ack carries the authoritative score. UI shows Saving-your-score only after ack (durable acceptance); until then the do-not-close state per UX. Unacked submissions persist in a localStorage outbox retried on interval and flushed via sendBeacon on pagehide - the UUID makes retries at-most-once, so an attempt is consumed at most once per Submit. Moodle writeback is decoupled: a grade outbox row is written in the same transaction and delivered asynchronously (24h SM-3 window).
- (decision) HANDOFF 12 RESOLVED - Curriculum Reference provenance: pipeline-derived during the plan stage from Grounding Chunk metadata (source document curriculum/syllabus tags; falls back to topic-derived label when metadata carries none). Stored in Lesson Script metadata as {value, source: pipeline|teacher}. Teacher CAN correct it in review: editable text field in the Review Workspace lesson-metadata header (within the A-28 text-editability map); pipeline-derived value retained alongside the override for provenance.
- (decision) A-14 RESOLVED as assumed: Simulation feature flag OFF for a Tenant means (a) generation pipeline emits no Simulation Blocks for new jobs, (b) Player renders existing Published Versions Simulation Blocks as Poster fallback cards (poster+parameter text, counts as viewed). Flag state ships in the playback-session bootstrap payload, evaluated at session start; toggling requires no redeploy (flags are tenant config rows read at runtime).
- (decision) Sanitiser: server-side Python allowlist sanitiser over defusedxml-parsed lxml tree (tag+attribute allowlist; strips script, event handlers, foreignObject, external refs/hrefs; rejects when un-conformable). Allowlist explicitly PRESERVES title, desc, role=img, aria-label, aria-labelledby per approved UX item 15. Same pass hosts the diagram label-legibility check (font-size/viewBox math at 360px container width) - fails generation, not publish. Client-side DOMPurify render-time pass in Player/chat as defense-in-depth only; the server pass is the gate.
- (decision) FR-27 events: append-only events table (typed columns: tenant_id, actor pseudonym, event_type, occurred_at, entity refs; JSONB detail) + separate cost_telemetry table (tokens in/out, computed cost, tenant, user, workload, cache hit/miss, latency, model id, request id). Taxonomy extended per approved UX items: diagram_review_completed, diagram_invalidated (cache evicted), plus mark-reviewed. Student identifiers pseudonymised via per-tenant HMAC of LMS user id (stable per user = [HARD] attribution preserved; raw id kept only where grade attribution requires it). Per-Block generation progress events are TRANSIENT job-state rows/SSE only (not FR-27 analytics events) - progress card consumes them live; absence degrades to coarse states.
- (decision) Showcase tier detection (approved amendment E): client-side, feature-based, demote-when-unsure - requires ALL of: WebGL ok, deviceMemory>=4, hardwareConcurrency>=4, connection effectiveType 4g, saveData false, prefers-reduced-data absent; any missing/undefined signal demotes to Full. Never user-agent sniffing. Enhancement chunks (webfonts, motion) load via dynamic import after first content paint, cache-forever asset URLs, never render-blocking, excluded from core-bundle CI budget.
- (decision) Generation idempotency (A-7 ratified): key = SHA-256 over (tenant_id, course_ref, topic normalised lowercase+whitespace-collapsed, guidance normalised identically, schema major version, pipeline config version); explicit Regeneration bypasses the key and always re-runs; key checked before enqueue so no LLM call on match.
- (decision) Tenant isolation mechanics: every request resolves a TenantContext before any data access; repositories require it (no default-tenant code path); PostgreSQL row-level security on tenant-owned tables via per-request session GUC as defense-in-depth; asset paths /tenants/{tenant_id}/... ; every cache key prefixed tenant:{id}; per-tenant CORS allowlist from tenant config; Operator endpoints are a separate router requiring operator role + explicit tenant parameter (auditable), matching the [HARD] no-cross-tenant-path rule.
- (decision) Quota/budget subsystem (V3 seam d): one generic spend ledger keyed (tenant, action_type, period) + policy table {action_type -> quota count | rate limit window | budget money}; diagram requests, generation jobs, and any future student-triggered generation are just action_types. Enforced in the adapter call path (pre-flight reserve, post-flight settle with real cost), not only UI. Budget exhaustion behavior per OQ-9: pause queue intake, diagrams cache-only, replay untouched, in-flight jobs finish and settle.
- (decision) V3 seams a/b/c in the Player: (a) BlockRegistry maps block.type -> renderer module, unknown types skipped and excluded from counts (FR-2); new types including dialogue turns = new registry entry + minor schema bump. (b) LessonDeliverySource interface: getScript() resolves the complete Published Version today AND exposes an async block iterator/subscription signature so an incremental source is a drop-in; Player consumes only the interface. (c) NarrationProvider interface {available(), speak(text,lang), pause(), stop(), events}; MVP registers SpeechSynthesisProvider incl. the bounded-start-failure downgrade rung; pre-generated/streaming TTS = new provider registration.
- (decision) Schema package: JSON Schema draft 2020-12 documents in /schema as the single source of truth, consumed unmodified by python-jsonschema (backend) and ajv (player/authoring toolchain); no codegen. A-26 ratified: minor bump = additive-only (new optional fields, new block types - old players ignore per FR-2), major bump = breaking (renderer contract change) with the defined cannot-play state; version string MAJOR.MINOR in the script; CI carries forward-compat fixtures (unknown block type + future version stamps) and a published-version corpus replay test.
- (decision) Auth surfaces: (1) Teacher: Launch Token exchange -> Authoring Session cookie (see handoff 16). (2) Student playback: mod_edonlesson server-to-server call with tenant API key mints a Playback Session (student pseudonym-scoped bearer token bound to lesson+attempt context) - the browser never holds tenant keys. (3) block_edon_ai: same tenant-API-key server-to-server pattern to the diagram endpoint. Tenant API keys: hashed at rest, two concurrently valid per tenant for rotation, revocable without downtime.
- (decision) Diagram service flow: normalise request text -> cache key (tenant, normalised prompt hash) -> cache hit returns stored sanitised SVG (zero-cost telemetry event, no rate-limit charge) -> miss: rate-limit + quota + budget pre-flight via ledger -> identity-stripped retrieval + LLM call (mini-tier workload, structured output, Mermaid-or-SVG intermediate allowed) -> render to SVG if needed -> sanitiser gate -> store + serve with FR-28 label text + altText/description. Report control -> review queue row; Mark invalid = queue clear + cache eviction + event; Mark reviewed = queue clear + event.
- (decision) Grade/completion writeback (V3 seam f): platform exposes an LMS-agnostic delivery outbox - attempt-grade and completion records with idempotent ids, GET pending + POST ack endpoints. mod_edonlesson consumes it from a Moodle scheduled task (cron pull + gradelib/completion write + ack); retries are re-polls until ack, satisfying A-13/SM-3 24h window without the platform knowing Moodle APIs. LTI 1.3 later = new consumer of the same outbox; platform core untouched.
- (decision) Storage: a small StorageDriver port (put/get/delete/stream/presign-or-internal-redirect) with launch driver = VPS filesystem under /var/lib/edon-lesson/assets/tenants/{tenant}/..., served through nginx X-Accel-Redirect after platform authz; S3-compatible driver is the documented scale-out swap. Publishing FREEZES assets: publish copies/hardlinks every referenced asset into the immutable published-version prefix and writes a manifest - published lessons are static + self-containable (V3 seam e; SCORM export later = zip manifest+assets+player).
- (assumption) MinIO rejected as launch object store to keep AGPL entirely out of the runtime stack (project-context section 5 lists no-AGPL for platform dependencies; a filesystem driver also matches single-VPS ops). S3-compatible driver kept as the swap point. Tagged for stakeholder visibility, not a blocker.
- (decision) Simulation dual-mode (OQ-5 seam): Simulation Block schema carries mode: template|freecode - template = {template_id, params} against a platform-owned parameterised template library; freecode = {code bundle ref, params manifest}. Both render through the same sandbox iframe + postMessage protocol, so the ship-vehicle choice is a pipeline config toggle, not a schema or player change. Sandbox: iframe sandbox=allow-scripts (never allow-same-origin), srcdoc-injected CSP default-src none, versioned postMessage protocol {hello/ready/params/state/error}, bounded-time readiness watchdog. Pre-publish checks (A-35, extended): headless load in CI-grade harness - loads without error, signals ready in time, declared params map to native controls (keyboard operability), stays within CPU/mem budget.
- (version) Verified 2026-07-17 (PyPI/vendor pages): FastAPI 0.139.2 (Pydantic-v2 native, active); Flask 3.1.3 (WSGI, async ties workers); Pydantic 2.13.4; SQLAlchemy 2.0.51 + Alembic 1.18.5; uvicorn 0.51.0; Procrastinate 3.9.0 (PG-backed, async, active; maintainer bus-factor caveat); Celery 5.6.3 (needs broker); arq maintenance-only; jsonschema 4.26.0 (2020-12 full); openai SDK 2.46.0 (base_url override standard interop); PostgreSQL 18 current stable (18.4, EOL 2030), 17 supported to 2029; Playwright-Python 1.61.0; httpx 0.28.1 (19 months no release, maintainer closed community channels - stable but frozen; used anyway as openai SDK dep + de facto standard, flagged).
- (decision) ADR-001 DECIDED: FastAPI (0.139.x) uniformly - async LLM/edon-rag workloads, SSE progress, Pydantic-v2 alignment with schema-first spine, OpenAPI for the versioned platform API, edon-rag operational precedent. Flask remains the team default OUTSIDE this repo; divergence deliberate and recorded. Hexagonal core stays framework-free; routers/DI are the only FastAPI-coupled layer. File: docs/adr/ADR-001-backend-framework.md.
- (decision) ADR-003 DECIDED: Procrastinate 3.x job queue on the platform PostgreSQL - transactional enqueue (jobs commit atomically with domain writes: draft rows, outbox rows, quiz acks), async-native, no Redis/RabbitMQ to operate, workers scale horizontally as extra systemd hosts on the same PG. Queues: generation/delivery/maintenance. Budget pause = intake pause on generation queue. Task defs are thin wrappers over core use-cases so a queue swap touches only the worker adapter. File: docs/adr/ADR-003-job-queue.md.
- (version) LLM landscape verified 2026-07-17 (vendor pages): frontier GA - claude-sonnet-5 (3/15, intro 2/10 to Aug 31), claude-opus-4-8 (5/25), claude-fable-5 (10/50), gpt-5.6-sol (5/30), gpt-5.6-terra (2.5/15), gemini-3.5-flash (1.5/9, Google GA flagship; gemini-3.1-pro still Preview). Mini GA - gemini-3.1-flash-lite (0.25/1.5), gpt-5.4-mini (0.75/4.5), claude-haiku-4-5 (1/5), gpt-5.6-luna (1/6). Open-weight 24GB INT4 class moved: Qwen3.6-35B-A3B (Apache-2.0, ~17.5GB INT4, vLLM>=0.19 guided decoding on OpenAI-compatible server) supersedes Qwen3-32B as class exemplar. Prompt caching + batch APIs GA at all three vendors (~0.1x cached reads, 50 percent batch).
- (decision) ADR-002 DECIDED (plan fixed; winners = dated addendum when benchmark runs, config-only): benchmark actual pipeline through the production adapter, zero code changes per candidate (doubles as the [HARD] adapter-rule proof + telemetry verification); >=12 topics x >=4 NCE subjects on frozen retrieval fixtures, 3 runs each; GA-models-only; decide on list price not intro. Bars: schema validity >=98 percent post-repair, citation coverage 100 percent, publishability rubric median >=4, SM-1 median/p90, cost vs 2-dollar alert; simulation free-code check pass >=70 percent else OQ-5 resolves to template-library default; diagram = cheapest clearing parse/sanitiser/legibility/accuracy bars. Re-run on migration trigger, SM-2 dip, new GA model, or 6-monthly. File: docs/adr/ADR-002-launch-model-benchmark-plan.md.
- (decision) Adapter shape forced by verified provider facts: consumers see ONE OpenAI-compatible async interface ([HARD] section 3 holds at the consumer boundary); internally two drivers - openai-compatible (OpenAI, Gemini compat endpoint, vLLM, OpenRouter via base_url) and anthropic-native translating driver, because Anthropic's compat layer is test-grade (ignores response_format/strict, drops prompt caching) and Claude 5-tier rejects temperature/top_p/top_k (workload params provider-conditional inside the adapter, sampling-param-free by default). Pipeline orders prompts stable-prefix-first (system + chunks before per-Block instructions) to exploit prefix caching within a Generation Job.
- (version) Moodle verified 2026-07-17 (moodledev.io/phpdoc/core source): stable 5.2 (Apr 2026), 5.1 supported to Oct 2026, 4.5 LTS security-only to Oct 2027, 5.3 LTS scheduled 2026-10-05; 5.1 moved codebase under /public (deploy paths only). gradelib grade_update() current for modules; completion = class-based core_completion activity_custom_completion (moodledev page stale, pattern confirmed from core mod_lesson source); Task API scheduled/adhoc current (legacy cron removed 4.3); JS = ES2015+ source transpiled to AMD via js_call_amd; core ships NO default CSP (site-local CSP possible); external-script embed norms unwritten (LTI/H5P use iframes); target=_blank launch unrestricted; plugin interface files must be GPLv3; new in 5.x: grade penalties framework, activity overview class, Bootstrap 5, FEATURE_MOD_PURPOSE required.
- (decision) mod_edonlesson targets Moodle 5.1+ (5.3 LTS as durable target; 4.5 LTS not targeted - security-only). Player embed = DIRECT IN-PAGE SCRIPT MOUNT, not iframe: a small AMD shim (js_call_amd) injects the platform player bundle script tag and calls EdonPlayer.mount into the activity content region - required by the UX Moodle Embedding Contract (inherit page scroll, self-managed height, no nested scrollbars); core has no default CSP so nothing blocks it; site-local CSP allowance for the platform origin becomes a documented tenant-onboarding requirement. Simulation iframes INSIDE the player keep their own sandbox+CSP regardless. Plugin implements FEATURE_MOD_PURPOSE, custom_completion class, grade_update via gradelib, scheduled task for outbox drain; activity-overview class noted as optional polish, not MVP scope.
- (assumption) Moodle site-admin CSP (third-party local_csp or manual headers) could block the in-page player script at a given college; mitigation is the documented onboarding allowlist requirement + the FR-12-style cannot-load state. Tagged for stakeholder visibility; iframe fallback deliberately NOT designed (violates embedding contract) unless this surfaces in the field.
- (version) Frontend verified 2026-07-17: React 19.2.7 (react+react-dom ~55-60kB gzip real-build); Vite 8.1.5 (Rolldown/Oxc; lib-mode IIFE; build.target + cssTarget must be set - default baseline-widely-available far too new for the floor); Preact 10.29.7 (~5kB core, compat ~+3kB, no React-19-only features); Three.js r185 (0.185.1, ~182kB gzip full, tree-shakeable; DRACOLoader official addon + wasm decoder); @gltf-transform/cli 4.4.1 (ecosystem-standard Draco pipeline); ajv 8.20.0 (Ajv2020 class, 2020-12 full); DOMPurify 3.4.12 (SVG profile); Playwright 1.61 (no first-class CPU throttle - CDP Emulation.setCPUThrottlingRate is the CI low-spec mechanism, Chromium-only).
- (version) Device-floor facts verified 2026-07-17: Play-updated Android 8 froze at Chrome/WebView 138 (Aug 2025); never-updated factory WebView ~Chrome 58-61; safe syntax baseline ES2017 (avoid ES2018+); native ESM Chrome 61+ so the embed ships IIFE; Vite build.target chrome61/es2017 + cssTarget chrome61. Android System WebView does NOT implement Web Speech API at all (caniwebview 2026-07-11 + open Chromium bug) - WebView-shell browsers land on the Floor narration rung (transcript default) by design; full Chrome on Android works with quirks the UX ladder already covers (gesture-required speak, voices-empty-until-onvoiceschanged, pause==cancel).
- (decision) Frontend stack: Authoring UI = React 19.2 + Vite 8 SPA (brand webfonts always). Player = React 19.2 in MVP (constitution section 2 'React for UI' - compliant default), built with Vite 8 lib-mode to a self-contained IIFE (EdonPlayer.mount global), syntax target ES2017/chrome61, cssTarget chrome61, zero runtime schema validation (scripts are server-validated; ajv is toolchain/CI only). Three.js viewer + Draco decoder wasm = lazy model3d chunk; simulation runtime = lazy chunk; Showcase fonts+motion = lazy enhancement chunk. DOMPurify (SVG profile) render-time pass as defense-in-depth in Player and chat card.
- (question) Preact option for the Player core (BATCH): Preact 10.29.7 aliased would cut the core runtime ~50kB gzip (~5kB vs ~55-60kB) - material on metered 3G at the Android-8 floor - but constitution section 2 says 'React for UI', so the swap needs an explicit stakeholder amendment. Default if unanswered: React stays; revisit only if the CI first-paint floor fails. Player code is renderer-registry + hooks-lite, so the swap later is contained to the player build config + compat alias.
- (decision) CI-enforced budgets (tunable config, launch values): Player CORE bundle <=150kB gzip (hard fail; contains runtime + shell + slide/narration/quiz/diagram renderers + delivery/narration/registry abstractions). Lazy chunks off-core: model3d viewer <=220kB gzip (three tree-shaken + DRACOLoader; wasm decoder fetched alongside first model), simulation runtime <=60kB, Showcase enhancement (Inter+PJS subsets + motion) <=300kB. Assets: Model3D glTF <=5MB transfer hard cap / <=3MB pipeline-preferred (Draco via gltf-transform; sizes shown are transfer size per UX); Simulation bundle <=1.5MB; poster images <=60kB each (lazy, near-viewport); diagram SVG <=150kB post-sanitise. Perceived-perf floor: lesson title + first Slide text <=5s on the CI low-spec profile (CDP CPU throttle 6x + 400kbps/400ms RTT network shaping, Chromium); skeletons only for waits >300ms. All values live in one budgets.json consumed by CI and the pipeline validators.
- (decision) Narration reality (validated): SpeechSynthesisProvider feature-detects and applies the UX bounded-start watchdog (no audible start within 3s of tap -> Floor transcript rung, control hidden); WebView-shell browsers (no Web Speech API) land on Floor by detection, not error. Voice pick: lang tag en-NG -> en-GB -> any en. pause==cancel quirk on Android: provider restarts utterance from current block text rather than exposing broken pause.
- (decision) ADR set written (docs/adr/): ADR-004 lesson storage/versioning (JSONB scripts, immutable published_versions with no UPDATE/DELETE grants, one mutable Draft with optimistic revision, publish = one transaction, duplicate-as-new-lesson); ADR-005 pipeline (pipes-and-filters as pipeline.yaml + prompt files, plan/block_content/assemble_validate stages, job_progress rows for the assembly showpiece, A-5 ungrounded fail, per-Block fan-out concurrency default 4); ADR-006 player embedding (IIFE EdonPlayer.mount, ES2017/chrome61, core<=150kB budgets.json, tier detection, preview parity); ADR-007 simulation (sandbox allow-scripts + CSP default-src none, postMessage protocol v1, dual-mode template|freecode in schema, Playwright headless pre-publish checks incl. keyboard operability); ADR-008 tenant isolation (TenantContext + RLS via session GUC, operator router carve-out, RLS-per-table CI gate); ADR-009 credentials (4 kinds, launch 120s single-use, authoring 8h/T-15min, playback 24h, HS256 per-tenant dual keys); ADR-010 attempts/scoring/outbox (handoffs 11+17 + seam f); ADR-011 data protection (identity-free adapter types, HMAC pseudonyms, retention.yaml 12mo, processors record); ADR-012 storage (StorageDriver port, filesystem + X-Accel-Redirect, publish freeze + manifest, MinIO excluded AGPL); ADR-013 governance (reserve->settle, action_type vocabulary, OQ-9 semantics, flags).
- (decision) Spine distilled to ARCHITECTURE-SPINE.md: paradigm hexagonal modular monolith + pipes-and-filters + registry player; AD-1..AD-20; conventions (UUIDv7, ISO-8601 UTC, camelCase script / snake_case API, error envelope, RLS-per-migration CI, import-linter core isolation); stack seed verified 2026-07-17; container/deployment/ERD/source-tree diagrams; capability map incl. explicit V3 seams table; Deferred list of 10 named items with revisit points.
- (decision) Structured Events canonical taxonomy fixed (AD-7): PRD FR-27 list + approved extensions diagram_review_completed, diagram_invalidated, operator_action, cost_alert, writeback_overdue; regeneration events ride the lesson_generated/generation_failed family with scope detail (stays inside PRD taxonomy). Progress events are transient job_progress rows, not FR-27 analytics events.
- (question) BATCH ITEM: ratify the four UX handoff resolutions (attempt unit AD-15/ADR-010; Curriculum Reference provenance = pipeline-derived + Teacher-editable metadata field; Launch Token 120s single-use -> 8h authoring session, T-15min warning, relaunch-into-same-Draft; quiz durable-ack submission) and the A-14 poster render resolution.
- (question) BATCH ITEM: ADR-001 FastAPI diverges from the Flask team standard (sanctioned by brief section 9 either-way clause) - confirm.
- (question) BATCH ITEM: benchmark fixture corpus for ADR-002 needs stakeholder-supplied sample curriculum materials (>=12 topics, >=4 subjects) + two human rubric reviewers; also confirm GA-only candidate rule and that winners land as a dated ADR-002 addendum pre-launch.
- (question) BATCH ITEM: OQ-5 mechanism proposed - dual-mode schema ships both; ADR-002 simulation benchmark picks the launch default (free-code needs >=70 percent check pass, else template default with free-code behind tenant flag). PRD says owner is Architect + stakeholder: confirm criteria and timing.
- (question) BATCH ITEM: numeric defaults for sign-off (all tunable config): authoring session 8h absolute / warning T-15min; launch token 120s; playback token 24h; sim readiness watchdog 10s; narration start watchdog 3s; core bundle 150kB gzip; model3d chunk 220kB; showcase chunk 300kB; glTF 5MB hard/3MB preferred; poster 60kB; diagram SVG 150kB; sim bundle 1.5MB / heap 128MB; first-content 5s on CI low-spec profile (CPU 6x, 400kbps/400ms); outbox drain 5min; delivery outbox pull+ack; retention 12mo default.
- (question) BATCH ITEM: edon-rag contract gaps WI-RAG-1 (citation-grade stable chunk/document metadata - verify FIRST against deployed openapi.json) and WI-RAG-2 (tag filtering; fallback = tenant-wide retrieval, still PRD-compliant) registered as integration work items - confirm routing to edon-rag owners.
- (event) project-context.md extension appended (section 9, additive only, pending sign-off): ADR index + new build rules (transactional enqueue, TenantContext law, RLS CI gate, closed event taxonomy, budgets.json, ES2017 floor, identity-free adapter types, publish freeze). No [HARD] rule touched.
- (event) Reconcile wave complete (4 subagents): reconcile-prd.md 0c/2h/8m/8l; reconcile-ux.md 1c/3h/3m/6l; reconcile-brief.md 0c/1h/3m/5l; reconcile-project-context.md 1c/1h/2m/8l. All criticals/highs and actionable mediums applied; lint_spine.py clean (4 false-positive lows = deliberate path notation).
- (change) Fixes applied from reconcile wave: ADR-014 added (player composition interfaces + ResultsSink completing SCORM seam e); gltf-transform v4 CLI EXCLUDED as Prosperity-PPL dual-licensed - MIT SDK packages (core/extensions/functions) called from our own ingest script instead (constitution section 5 compliance); no-TypeScript CI lint gate + uv lockfile decision added to conventions and project-context section 9; idempotency fingerprint scoped per (tenant, OWNER) fixing OQ-13 draft-privacy collision; outbox ack-with-status (applied|failed) so writeback_failure/writeback_retry FR-27 events are real; curriculumRef {value, source} teacher-correctable landed in AD-2/ADR-004/ADR-005; Constrained-tier predicate defined (deviceMemory<=2 or undefined -> halved asset ceiling; load failure routes to Constrained); AD-21 added (diagram governance loop incl. Mark-invalid tenant cache eviction + zero-cost cache-hit telemetry emission point); simulation poster = headless capture at sim:ready (ADR-007); no-AI-raster poster fence encoded as pipeline rule (AD-17); AD-2 major-version clarified (cannot-play only for scripts NEWER than player; shipped majors never lose support); shared text normaliser decided (NFC/casefold/whitespace/trim/punct); asset:// id indirection so publish never alters previewed script bytes; default per-tenant generation concurrency 1 preserving A-29 one-job overrun bound; telemetry field contract enumerated in AD-19; SM computability widened to stored platform data (events+telemetry+domain records), cost still telemetry-alone; event taxonomy relabelled - operator_action/cost_alert/writeback_overdue are architecture additions PENDING SIGN-OFF; dev-only token-mint CLI for pre-Moodle epics (ADR-009); CI low-spec profile gains memory cap ~1.5GB; defusedxml/lxml/anthropic SDK added to stack seed; embeddings-key note in AD-3; AD-14 four-credential rule scoped to MVP.
- (question) BATCH ITEM (new from reconcile): NFR-3 secrets-via-env interpretation - platform secrets env-only; per-tenant credentials are hashed or KEK-encrypted DB rows (KEK in env). Confirm this reading of constitution section 4.
- (question) BATCH ITEM (new from reconcile): three architecture-added event types (operator_action, cost_alert, writeback_overdue) extend the FR-27 canonical taxonomy - confirm (the UX-approved diagram_review_completed/diagram_invalidated were already sanctioned).
- (question) BATCH ITEM (new from reconcile): NFR-9 free-text passthrough interpretation - topic/guidance/diagram request text pass to the LLM as content, with no PII scrubbing attempted in MVP (identity STRIPPING is structural; free text is user-authored). Confirm.
- (event) Reviewer gate wave 1: session limit killed all 3 lenses mid-run; review-adversarial.md survived complete (3c/8h/7m/3l - verdict revise-before-handoff); rubric + tech-currency lenses re-dispatched after session resume.
- (change) All adversarial-lens closures applied: NEW AD-22 playback bootstrap contract (single token-authed bootstrap carrier; canonical mount {scriptUrl, token, locale}; session response plugin-display-only; observer sessions read-only); NEW AD-23 draft concurrency law (plan-stage stable Block ids preserved through regen+publish; regen participates in revision sequence with {block_id,new_revision} rebase; normative 409 recovery = per-Block three-way rebase never discard/replay; restore-after-expiry stages proposals with base revision; publish If-Match + revision-pinned checks); NEW AD-24 progress-stream contract (state enum, block_id refs, snapshot-then-tail SSE, terminal events, display_key via language catalog); AD-15 tightened (attempt scope (lesson_id,activity_ref); player-declared completion denominator, server renderability inference forbidden; one scored submission per block first-ack-wins; observer sessions); AD-12 tightened (param descriptor shape, set-param form, sim:state echo, data-edon-param marker, prompts derived from protocol doc); AD-3+AD-8 tightened (governance_ref carrying action_type; two named enforcement points; governance_state in bootstrap; queued-job budget_paused fail-fast); AD-2 (citation required-set = guaranteed fields, transferSize on asset refs); AD-9 (hashed cache keys, storage key charset); AD-11 (budgets.json repo-root, own schema, bytes, four named consumers); AD-17 (normalise_key single function + fixtures); AD-18 (flags never strip content); AD-20 (rendering split chat-rendered vs first-party code+params); AD-21 (review-row shape, diagram_id-keyed invalidation). Companions synced: ADR-006/010/014, mod-edonlesson contract (bootstrap, activity scoping, observer, curriculum_ref flat string).
- (decision) Attempt-unit resolution AMENDED by adversarial finding H1: attempts/limits/completion scoped to (lesson_id, activity_ref) - supersedes the earlier per-Lesson-global phrasing; UX 'attempt counts are per-Lesson' holds within one activity context. Carried into the sign-off batch item for handoff 11.
- (event) Reviewer gate wave 2 complete: review-rubric.md PASS WITH CONDITIONS (0c/2h/4m/6l; dimensions 6 strong / 2 adequate); review-tech-currency.md CONDITIONAL PASS (1c/1h/3m/5l + 14-row verified-clean ledger). All criticals/highs and actionable mediums/lows applied. lint_spine.py: 8 lows, all deliberate path/protocol notation false-positives.
- (change) CORRECTION (tech-currency C-1, overrides reconcile-project-context C1): @gltf-transform CLI 4.4.x is MIT at the pinned version (repo LICENSE + npm; early-v4 Prosperity relicensing reverted) - CLI exclusion REVERTED; replaced with a general licence-audit CI gate on both lockfiles vs the section 5 allowlist (rubric M4). Other wave-2 fixes: operator credential defined as FIFTH kind (rubric H2; AD-14/ADR-009/ADR-008, project-context section 9); old-engine floor actively enforced - targeted built-in polyfills in core budget (globalThis, queueMicrotask, Promise.finally), browserslist compat-lint, no import.meta.url in IIFE (Vite 8 drops its polyfill - base URL via document.currentScript), one real old-engine smoke as pre-launch hardening item (tech H-1, M-1); Simulation schema fields camelCased templateId/bundleRef (rubric M1); Authoring SPA composition convention row (react-router + fetch wrappers, no state lib); test/lint toolchain fixed (pytest+ruff, Vitest+ESLint+Prettier, Playwright); API list pagination = limit + opaque cursor; AD-20 gains i18n lint enforcement + surface.component.slug keys; deploy-mechanics Deferred row; device-bound overclaim dropped from playback token; ADR-002 mini rows gpt-5.4-mini + gemini-3.1-flash-lite marked re-verify-at-benchmark, Qwen3.6 row sourced (HF model card), Sonnet-5 sampling nuance corrected to non-default-values; model library index gains curriculum_tags (FR-16).
- (event) Run complete pending sign-off: spine (AD-1..AD-24) + ADR-001..014 + 3 integration contracts + project-context section 9 extension all consistent; artifacts held at status draft per stakeholder run discipline; consolidated sign-off batch presented 2026-07-18.

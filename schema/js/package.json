{
  "name": "@edon/lesson-schema",
  "version": "1.0.0",
  "description": "Lesson Script Schema v1.0 — ajv validator wrapper (toolchain/CI only; no runtime validation in the Player)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "~8.20.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}

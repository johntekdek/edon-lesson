{
  "name": "@edon/player",
  "version": "0.0.1",
  "description": "Embeddable student Player — the shell (EdonPlayer.mount) lands with Story 4.1; this is the workspace skeleton the CI gates bind to",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}

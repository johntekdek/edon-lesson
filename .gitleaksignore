# Per-finding allowlist for the gitleaks gate (Story 1.2 triage, 2026-07-19).
# Every entry needs a justification — never allowlist a real secret; rotate it instead.
# NB: describe findings, don't quote them — quoted trigger text re-trips the scanner.
# NB: fingerprints embed commit SHAs — a history rewrite invalidates them. The
# first-push recovery rebase (2026-07-19) rewrote all SHAs, and the pre-rewrite
# history stays reachable via local branches, so BOTH generations are listed.

# False positive: planning-artifact prose (a list of operator-managed nouns in the
# epics doc) happens to match the generic-api-key regex. No secret present.
# pre-rewrite commit:
fc3e4bec0b934ddf26542f6205e3c58f88b9ae1b:_bmad-output/planning-artifacts/epics.md:generic-api-key:104
# rewritten commit (the one on origin/main):
062c853704d751f7aa04306a5ac7cd3281863722:_bmad-output/planning-artifacts/epics.md:generic-api-key:104

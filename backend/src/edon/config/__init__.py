"""Runtime configuration code.

Note: the architecture spine's source tree also names a config/ home for pipeline.yaml,
prompts/, retention.yaml — those are data files that land with their stories (Epic 2+).
This package holds config *code* only; no placeholder YAML lives here.
"""
